// const { body, param } = require('express-validator');

// exports.validateTodoId = param('id').isInt({ min: 1 });

// exports.validateTodoBody = [
//   body('text').isString().trim().notEmpty(),
//   body('completed').isString().trim().notEmpty(),
// ];

// exports.validateUpdateTodoBody = [
//   body('text').isString().trim().optional(),
//   body('completed').isString().trim().optional(),
// ];
